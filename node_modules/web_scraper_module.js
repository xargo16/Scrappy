//External modules
const request = require('request');

//My modules
const html_parser = require('html_parser_module');

function printDetailsAboutCompanyFromWebsiteUrl(websiteUrl){
  request(websiteUrl, function (error, response, body) {
    console.log(html_parser.getDetailsAboutCompanyFromHtmlBody(body));
  });
}

module.exports = {
  printDetailsAboutCompanyFromWebsiteUrl: printDetailsAboutCompanyFromWebsiteUrl
};
