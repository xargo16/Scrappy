//External modules
const Knwl = require("./knwl.js");
const cheerio = require("./cheerio");

//variables
var knwlInstance = new Knwl('english');
var listOfCompanyDetails = [];

function getDetailsAboutCompanyFromHtmlBody(body){
    const $ = cheerio.load(body);

    $('*').each(function(){
          var textFromElement = $(this).text();
          if(stringContainsDataAboutCompany(textFromElement)){
            listOfCompanyDetails.push(textFromElement);
          }
    });

    console.log(listOfCompanyDetails);
}

function stringContainsDataAboutCompany(string){
  return false;
}

module.exports = {
  getDetailsAboutCompanyFromHtmlBody: getDetailsAboutCompanyFromHtmlBody
};
